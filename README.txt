KC Council 12906 BBQ Photo Update

Extract this ZIP directly into:
C:\Users\thada\kc12906-website

Allow Windows to merge folders and replace app\events\page.tsx and
app\events\bbq\page.tsx.

Then run from PowerShell inside the project folder:

git add .; git commit -m "Add Council BBQ photos"; git push

Vercel should deploy the update automatically.
